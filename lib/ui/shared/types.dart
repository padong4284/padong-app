enum ButtonType {
  ROUNDED,
  STADIUM,
  CIRCLE,
}

enum ButtonSize {
  GIANT,
  LARGE,
  REGULAR,
  SMALL,
}

enum UsernamePosition {
  BOTTOM,
  RIGHT_CENTER,
}

enum InputType {
  ROUNDED,
  PLAIN,
  UNDERLINE,
}

enum SwitchButtonType {
  BORDER,
  SHADOW,
  TOOLTIP,
}

enum FriendTileType {
  LIST,
  INVITE,
}

enum BottomSenderType {
  ARGUE,
  REVIEW,
  REPLY,
  CHAT,
}
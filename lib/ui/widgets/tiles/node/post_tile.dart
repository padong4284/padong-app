import 'package:padong/ui/widgets/tiles/node/node_base_tile.dart';

class PostTile extends NodeBaseTile {
  PostTile(id) : super(id);

  @override
  void moreCallback() {
    // same as post more callback
  }
}

enum ButtonType {
  ROUNDED,
  STADIUM,
  CIRCLE,
}

enum ButtonSize {
  GIANT,
  LARGE,
  REGULAR,
  SMALL,
}

enum InputType {
  ROUNDED,
  PLANE,
  UNDERLINE,
}

enum SwitchButtonType {
  BORDER,
  SHADOW,
  TOOLTIP,
}